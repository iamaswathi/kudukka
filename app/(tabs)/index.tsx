// index.tsx (React Native CLI or Expo ejected only)
import App from '@/App';
import { registerRootComponent } from 'expo';
// import App from './App';

// This tells Expo or RN CLI to start from App.tsx
registerRootComponent(App);